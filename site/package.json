{
  "name": "richmond-food-meta-site",
  "version": "0.1.0",
  "private": true,
  "description": "Static site for the Richmond BC Chinese food meta guide. Compiled from data/** at the repo root.",
  "type": "module",
  "scripts": {
    "build": "node scripts/compile.mjs",
    "watch": "node scripts/compile.mjs --watch",
    "serve": "python -m http.server 8137"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
